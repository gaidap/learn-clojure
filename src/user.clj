(ns user
  (:require [learn-clojure.core]
            [learn-clojure.representation]
            [learn-clojure.interaction]))

;; Optional: initialisiert spezifische Funktionen oder startet benötigte Ressourcen
(defn init []
  ;; Code zur Initialisierung, z.B. Datenbankverbindung, Setup-Aktionen, etc.
  (println "All namespaces loaded and initialization complete."))

(init)
